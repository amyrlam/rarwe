{{page-title "Rock & Roll with Octane"}}

<div class="bg-blue-900 text-gray-100 p-8 h-screen">
  <div class="h-12 flex items-center mb-4 border-b-2">
    <a href="#">
      <h1 class="font-bold text-2xl">
        Rock & Roll <span class="font-normal">with</span> Octane
      </h1>
    </a>
  </div>
  <nav class="h-8 flex items-center mb-8">
    <LinkTo class="hover:text-gray-500" @route="bands">Bands</LinkTo>
    <LinkTo class="hover:text-gray-500 ml-4" @route="songs">Songs</LinkTo>
  </nav>

  {{outlet}}
</div>
